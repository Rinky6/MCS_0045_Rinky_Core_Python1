'''

-->JSON is javscript object notation is a standardized format that allows exchanging of data
   across the internet.
the JSON module has some methods and properties in it. see below :
'''

import json
print(dir(json))